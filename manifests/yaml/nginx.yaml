        
        apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/instance: nginx
  name: argo-nginx
  namespace: default
spec:
  replicas: 1
  template:
    metadata:
      labels:
        editor: argo
    spec:
      containers:
        - image: nginx
          name: nginx

